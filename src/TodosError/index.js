import { Box, Typography } from '@mui/material';
import React from 'react';
function TodosError(){ 
    return (
      <Box>
        <Typography>Error al cargar</Typography>
      </Box>
    );
}
export {TodosError};